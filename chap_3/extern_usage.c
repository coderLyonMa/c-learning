#include "shell_sort.c"

void prt();

int main()
{
    prt();
    return 0;

    initv(v, MAX_SIZE);
    shellsort(v, MAX_SIZE);
    prt_arr(v, MAX_SIZE);
}

void prt()
{
    foo += 1;
    printf("%d", foo);
}
